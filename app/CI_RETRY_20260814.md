PVNetwork Android CI diagnostic retry.

Purpose: retrigger the existing real Android build without changing product source so the current toolchain failure can be inspected against the persisted state in docs/PROJECT_STATE.md and docs/KNOWN_ISSUES.md.
